# Step 4 — Public read-only view

Not built yet.

One static HTML file, mobile first, Cytoscape.js from a CDN. Force-directed
graph rather than a hierarchical tree, because intermarriage means multiple
paths to the same ancestor.
